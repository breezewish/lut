# Web UI Redesign End-to-End Tests

- Switching the application from Dark to Light mode updates the shell and restores Light mode after reload.
- A selected RAW uses queue, comparison, adjustment, and output regions while preserving the existing processed preview and export journey.
- Medium and mobile widths preserve workflow order, keep import and export reachable, and stack previews only below 35rem.
