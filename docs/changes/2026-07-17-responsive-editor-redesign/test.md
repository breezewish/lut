# Responsive Editor Redesign Tests

- A 60-event exposure burst finishes input dispatch within 1.1 seconds, publishes at least 12 exact-color interaction frames, and settles the final 1024px recipe.
- A checksum-verified 9568 × 6376 Sony RAW accepts at least 45 drag inputs during decode under 4× CPU throttling with frame-gap p95 below 25 ms and no gap reaching 100 ms.
- Light, Dark, desktop, and mobile screenshots retain the same compact, borderless, shadowless component vocabulary.
- Export disables on the first pending exposure event and re-enables only after the exact final settled recipe.
